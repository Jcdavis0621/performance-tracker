{
  "name": "performance-tracker",
  "version": "1.0.0",
  "description": "Full-stack performance review tracker with React frontend and Node.js backend",
  "private": true,
  "main": "backend/src/server.js",
  "scripts": {
    "preinstall": "npm install --prefix backend && npm install --prefix frontend",
    "start": "cd backend && npm run start",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build:backend": "echo 'Backend does not require build'",
    "build:frontend": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
