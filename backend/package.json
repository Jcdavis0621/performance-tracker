{
  "name": "performance-tracker-backend",
  "version": "1.0.0",
  "description": "Performance Tracker API",
  "main": "src/server.js",
  "scripts": {
    "preinstall": "cd backend && npm install && cd ../frontend && npm install",
    "prestart": "cd backend && npm run start",
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "migrate": "node src/migrate.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.8.0",
    "dotenv": "^16.0.3",
    "bcrypt": "^5.1.0",
    "jsonwebtoken": "^9.0.0",
    "cors": "^2.8.5",
    "helmet": "^7.0.0",
    "express-validator": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
